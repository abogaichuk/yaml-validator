package com.example.yamlvalidator.grammar;

public enum KeyWord {
    TYPE,
    ITEMS,
    ENUM,
    ONEOF,
    ANYOF,
    PROPERTIES,
    PATTERN,
    DESCRIPTION,
    DEFAULT,
    REQUIRED,
    EXAMPLE,
    BYPASS,
    MIN,
    MAX,
    LIST,
    AFTER,
    BEFORE;
}

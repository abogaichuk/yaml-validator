package com.example.yamlvalidator.grammar;

public enum KeyWord {
    TYPE,
    TYPES,
    ITEMS,
    ENUM,
    UNIQUE,
    ONEOF,
    ANYOF,
    PROPERTIES,
    PATTERN,
    DESCRIPTION,
    DEFAULT,
    REQUIRED,
    EXAMPLE,
    BYPASS,
    MIN,
    MAX,
    LIST,
    AFTER,
    BEFORE;
}
